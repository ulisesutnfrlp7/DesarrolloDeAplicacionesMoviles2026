{
  "name": "opb-virtual-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test lib/__tests__/*.test.js",
    "emulators": "firebase emulators:start --only functions,firestore"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "expo-server-sdk": "^3.15.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
